Q.Search in Rotated Sorted Array

ex : Input: arr[] = [5, 6, 7, 8, 9, 10, 1, 2, 3], key = 3
     Output: 8
     Explanation: 3 is found at index 8.

code :
int search(vector<int>& arr, int key) {
       
       int n=arr.size();
       for(int i=0;i<n;i++)
       {
           if(key==arr[i])
           {
               return i;
           }
       }
       return -1;
    }
